server:
  port: 8082
  servlet:
    context-path: '/comparison'

spring:
  datasource:
    url:  jdbc:postgresql://localhost:5432/item-comparison
    username: postgres
    password: banco123
  jpa:
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.PostgreSQLDialect
      jdbc:
        lob.non-contextual-creation: true
    properties:
      hibernate:
        show_sql: true
